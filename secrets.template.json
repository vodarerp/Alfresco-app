{
  // =====================================================================================
  // USER SECRETS TEMPLATE
  // =====================================================================================
  // This file shows the structure for sensitive configuration that should NEVER be
  // committed to source control.
  //
  // HOW TO USE:
  // 1. Right-click on the project in Visual Studio -> Manage User Secrets
  // 2. Copy the structure below and fill in real values
  // 3. For production, use Azure Key Vault or environment variables
  //
  // COMMAND LINE (dotnet CLI):
  // dotnet user-secrets set "ClientApi:ApiKey" "your-actual-api-key"
  // dotnet user-secrets set "DutApi:ApiKey" "your-actual-api-key"
  // dotnet user-secrets set "ConnectionStrings:OracleConnection" "your-connection-string"
  // =====================================================================================

  "ClientApi": {
    "ApiKey": "YOUR_CLIENT_API_KEY_HERE",
    "BaseUrl": "https://client-api.your-bank.com"
  },

  "DutApi": {
    "ApiKey": "YOUR_DUT_API_KEY_HERE",
    "BaseUrl": "https://dut-api.your-bank.com"
  },

  "AlfrescoRead": {
    "BaseUrl": "https://old-alfresco.your-bank.com",
    "Username": "YOUR_USERNAME_HERE",
    "Password": "YOUR_PASSWORD_HERE"
  },

  "AlfrescoWrite": {
    "BaseUrl": "https://new-alfresco.your-bank.com",
    "Username": "YOUR_USERNAME_HERE",
    "Password": "YOUR_PASSWORD_HERE"
  },

  "ConnectionStrings": {
    "OracleConnection": "Data Source=(DESCRIPTION=(ADDRESS=(PROTOCOL=TCP)(HOST=oracle-db.your-bank.com)(PORT=1521))(CONNECT_DATA=(SERVICE_NAME=MIGRATIONDB)));User Id=MIGRATION_USER;Password=YOUR_DB_PASSWORD;",
    "OracleReadOnlyConnection": "Data Source=(DESCRIPTION=(ADDRESS=(PROTOCOL=TCP)(HOST=oracle-db-replica.your-bank.com)(PORT=1521))(CONNECT_DATA=(SERVICE_NAME=MIGRATIONDB)));User Id=MIGRATION_READER;Password=YOUR_READER_PASSWORD;"
  }
}
