<UserControl x:Class="Alfresco.App.UserControls.Main"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Alfresco.App.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid x:Name="MainGrid" Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>
                <DataGrid x:Name="dataGridA" Grid.Row="0"  Margin="10" AutoGenerateColumns="false" ItemsSource="{Binding Folders}" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="ID" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Id}" TextWrapping="Wrap"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Name" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Name}" TextWrapping="Wrap"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <DataGrid x:Name="dataGridOracle" Grid.Row="1"  Margin="10" AutoGenerateColumns="false" ItemsSource="{Binding Documents}" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="ID" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Id}" TextWrapping="Wrap"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="NodeId" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding NodeId}" TextWrapping="Wrap"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
            
            <Grid Grid.Column="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <!--<Button Grid.Column="0" x:Name="btnSearch" Content="Search" Height="40" Click="btnSearch_Click" ></Button>
                <Button Grid.Column="1" x:Name="btnLoad" Content="InserS" Height="40" Click="btnInsert_Click" ></Button>
                <Button Grid.Column="2" x:Name="btnInsert" Content="InsertM"  Height="40" Click="btnInsert_Click_1" ></Button>-->
                <Button x:Name="btnFolders" Content="CreateFolders" Click="btnFolders_Click" Height="40"/>
                <Button Grid.Column="1" x:Name="btnGetFolders" Content="GetFolders" Click="btnGetFolders_Click" Height="40"/>
                <Button Grid.Column="2" x:Name="btn2" Content="testing" Click="GetDocsFromFolder" Height="40"/>
                <Button Grid.Column="3" x:Name="btnMove" Content="Move"  Height="40" Click="btnMove_Click" ></Button>



            </Grid>
        </Grid>
        <local:StatusBarUC Grid.Row="1" Height="40"/>

    </Grid>
</UserControl>
