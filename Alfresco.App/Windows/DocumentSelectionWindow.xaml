<Window x:Class="Alfresco.App.Windows.DocumentSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Select Documents for Migration"
        Height="700" Width="1100"
        WindowStartupLocation="CenterOwner"
        Loaded="Window_Loaded">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2C3E50" Padding="15" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="Document Selection for Migration"
                           Foreground="White" FontSize="18" FontWeight="Bold"/>
                <TextBlock Text="Select one or more document types to search for migration (by ecm:docDesc field)"
                           Foreground="#ECF0F1" FontSize="12" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Search + Filter Bar -->
        <Border Grid.Row="1" Background="#ECF0F1" Padding="10" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="30"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Search:"
                           FontSize="12" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox Grid.Column="1"
                         x:Name="txtSearch"
                         FontSize="12" Padding="5"
                         TextChanged="txtSearch_TextChanged"
                         ToolTip="Search by document name or code"/>
                <TextBlock Grid.Column="3" Text="Tip Dosijea:"
                           FontSize="12" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ComboBox Grid.Column="4"
                          x:Name="cmbTipDosijea"
                          FontSize="12" Padding="5,3"
                          ToolTip="Filter by dossier type"/>
                <!-- View Mode toggle -->
                <StackPanel Grid.Column="6" Orientation="Horizontal" VerticalAlignment="Center">
                    <RadioButton x:Name="rbStandard" Content="Standardni"
                                 FontSize="12" IsChecked="True"
                                 Margin="0,0,12,0"
                                 Checked="rbStandard_Checked"/>
                    <RadioButton x:Name="rbGrouped" Content="Grupisani"
                                 FontSize="12"
                                 Checked="rbGrouped_Checked"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content: Two Panels -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Panel: Database Documents -->
            <Border Grid.Column="0" BorderBrush="#BDC3C7" BorderThickness="1" Background="White">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- ===== STANDARD MOD: Headers ===== -->
                    <Border x:Name="pnlStandardHeader" Grid.Row="0" Background="#34495E" Padding="10,8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="40"/>
                                <ColumnDefinition Width="70"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="130"/>
                                <ColumnDefinition Width="60"/>
                            </Grid.ColumnDefinitions>
                            <CheckBox Grid.Column="0"
                                      x:Name="chkSelectAll"
                                      VerticalAlignment="Center"
                                      Checked="chkSelectAll_Checked"
                                      Unchecked="chkSelectAll_Unchecked"
                                      ToolTip="Select/Deselect All"/>
                            <TextBlock Grid.Column="1" Text="Code"
                                       Foreground="White" FontWeight="SemiBold" FontSize="11"/>
                            <TextBlock Grid.Column="2" Text="Document Name"
                                       Foreground="White" FontWeight="SemiBold" FontSize="11"/>
                            <TextBlock Grid.Column="3" Text="Tip Dosijea"
                                       Foreground="White" FontWeight="SemiBold" FontSize="11"/>
                            <TextBlock Grid.Column="4" Text="Count"
                                       Foreground="White" FontWeight="SemiBold" FontSize="11"/>
                        </Grid>
                    </Border>

                    <!-- ===== GRUPISANI MOD: Headers (hidden by default) ===== -->
                    <Border x:Name="pnlGroupedHeader" Grid.Row="0" Background="#34495E" Padding="10,8"
                            Visibility="Collapsed">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="36"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="70"/>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="100"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="1" Text="Naziv dokumenta"
                                       Foreground="White" FontWeight="SemiBold" FontSize="11"/>
                            <TextBlock Grid.Column="2" Text="Varijanti"
                                       Foreground="White" FontWeight="SemiBold" FontSize="11"
                                       HorizontalAlignment="Right"/>
                            <TextBlock Grid.Column="3" Text="Br. dok."
                                       Foreground="White" FontWeight="SemiBold" FontSize="11"
                                       HorizontalAlignment="Right"/>
                            <TextBlock Grid.Column="4" Text="TipDosijea"
                                       Foreground="White" FontWeight="SemiBold" FontSize="11"
                                       HorizontalAlignment="Center"/>
                        </Grid>
                    </Border>

                    <!-- ===== STANDARD MOD: List ===== -->
                    <ScrollViewer x:Name="pnlStandardList" Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="lstDocuments">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="#E0E0E0" BorderThickness="0,0,0,1"
                                            Background="White" Padding="10,6">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#F5F5F5"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="40"/>
                                                <ColumnDefinition Width="70"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="130"/>
                                                <ColumnDefinition Width="60"/>
                                            </Grid.ColumnDefinitions>

                                            <CheckBox Grid.Column="0"
                                                      IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                                      VerticalAlignment="Center"
                                                      Checked="DocumentCheckBox_Changed"
                                                      Unchecked="DocumentCheckBox_Changed"/>

                                            <TextBlock Grid.Column="1"
                                                       Text="{Binding SifraDokumenta}"
                                                       FontSize="10" VerticalAlignment="Center"
                                                       FontFamily="Consolas"/>

                                            <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Naziv}"
                                                           FontSize="11" FontWeight="SemiBold"
                                                           TextTrimming="CharacterEllipsis"/>
                                                <TextBlock Text="{Binding NazivDokumenta}"
                                                           FontSize="9" Foreground="#7F8C8D"
                                                           Margin="0,2,0,0"
                                                           TextTrimming="CharacterEllipsis"/>
                                            </StackPanel>

                                            <TextBlock Grid.Column="3"
                                                       Text="{Binding TipDosijea}"
                                                       FontSize="10" VerticalAlignment="Center"
                                                       HorizontalAlignment="Center"
                                                       Foreground="#2C3E50"
                                                       TextTrimming="CharacterEllipsis"/>

                                            <TextBlock Grid.Column="4"
                                                       Text="{Binding BrojDokumenata}"
                                                       FontSize="10" VerticalAlignment="Center"
                                                       HorizontalAlignment="Center"
                                                       Foreground="#3498DB"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <!-- ===== GRUPISANI MOD: List (hidden by default) ===== -->
                    <ScrollViewer x:Name="pnlGroupedList" Grid.Row="1" VerticalScrollBarVisibility="Auto"
                                  Visibility="Collapsed">
                        <ItemsControl x:Name="lstGroupedDocuments">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="#E0E0E0" BorderThickness="0,0,0,1"
                                            Background="{Binding RowBackground}" Padding="10,6">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="36"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="70"/>
                                                <ColumnDefinition Width="80"/>
                                                <ColumnDefinition Width="100"/>
                                            </Grid.ColumnDefinitions>

                                            <!-- Checkbox za oba tipa (GROUP i SINGLE) -->
                                            <CheckBox Grid.Column="0" VerticalAlignment="Center"
                                                      IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                                      Checked="GroupedDocumentCheckBox_Changed"
                                                      Unchecked="GroupedDocumentCheckBox_Changed"/>

                                            <!-- Naziv -->
                                            <TextBlock Grid.Column="1"
                                                       Text="{Binding DisplayNaziv}"
                                                       FontSize="11"
                                                       FontWeight="{Binding NameWeight}"
                                                       VerticalAlignment="Center"
                                                       TextTrimming="CharacterEllipsis"/>

                                            <!-- Varijanti -->
                                            <TextBlock Grid.Column="2"
                                                       Text="{Binding VariantCountDisplay}"
                                                       FontSize="10" VerticalAlignment="Center"
                                                       HorizontalAlignment="Right"
                                                       Foreground="#8E44AD"/>

                                            <!-- Br. dok. -->
                                            <TextBlock Grid.Column="3"
                                                       Text="{Binding TotalDocumentsDisplay}"
                                                       FontSize="10" VerticalAlignment="Center"
                                                       HorizontalAlignment="Right"
                                                       Foreground="#3498DB"/>

                                            <!-- TipDosijea -->
                                            <TextBlock Grid.Column="4"
                                                       Text="{Binding TipDosijea}"
                                                       FontSize="10" VerticalAlignment="Center"
                                                       HorizontalAlignment="Center"
                                                       Foreground="#2C3E50"
                                                       TextTrimming="CharacterEllipsis"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- Right Panel: Selected Documents -->
            <Border Grid.Column="2" BorderBrush="#BDC3C7" BorderThickness="1" Background="White">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Selected Header -->
                    <Border Grid.Row="0" Background="#27AE60" Padding="10,8">
                        <TextBlock x:Name="txtSelectedHeader"
                                   Text="Selected Documents (0)"
                                   Foreground="White" FontWeight="SemiBold" FontSize="12"/>
                    </Border>

                    <!-- Selected List -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="lstSelected">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="#E0E0E0" BorderThickness="0,0,0,1"
                                            Background="White" Padding="8,6">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#FFF5F5"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="28"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <!-- Tag [G] ili [D] -->
                                            <Border Grid.Column="0"
                                                    Background="{Binding TagColor}"
                                                    CornerRadius="3" Padding="4,2"
                                                    VerticalAlignment="Center"
                                                    HorizontalAlignment="Left">
                                                <TextBlock Text="{Binding TagLabel}"
                                                           Foreground="White" FontSize="10"
                                                           FontWeight="Bold"/>
                                            </Border>

                                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="6,0">
                                                <TextBlock Text="{Binding DisplayName}"
                                                           FontSize="11" FontWeight="SemiBold"
                                                           TextTrimming="CharacterEllipsis"/>
                                                <TextBlock Text="{Binding TipDosijea}"
                                                           FontSize="9" Foreground="#7F8C8D"
                                                           TextTrimming="CharacterEllipsis"/>
                                            </StackPanel>

                                            <Button Grid.Column="2"
                                                    Content="X"
                                                    Click="btnRemoveSelected_Click"
                                                    Width="24" Height="24"
                                                    FontSize="11" FontWeight="Bold"
                                                    Background="#E74C3C" Foreground="White"
                                                    BorderThickness="0"
                                                    VerticalAlignment="Center"
                                                    ToolTip="Remove from selection"
                                                    Cursor="Hand"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

        <!-- Footer with paging, buttons and selection count -->
        <Border Grid.Row="3" Background="#ECF0F1" Padding="15" Margin="0,15,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Paging Info and Controls -->
                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               x:Name="txtPagingInfo"
                               Text="Showing 0 of 0 documents"
                               FontSize="11"
                               VerticalAlignment="Center"
                               Foreground="#7F8C8D"/>

                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button x:Name="btnPrevPage"
                                Content="&#9664; Previous"
                                Click="btnPrevPage_Click"
                                Padding="10,5" Margin="0,0,5,0"
                                Background="#3498DB" Foreground="White"
                                BorderThickness="0"
                                FontSize="11"/>
                        <Button x:Name="btnNextPage"
                                Content="Next &#9654;"
                                Click="btnNextPage_Click"
                                Padding="10,5"
                                Background="#3498DB" Foreground="White"
                                BorderThickness="0"
                                FontSize="11"/>
                    </StackPanel>
                </Grid>

                <!-- Selection Count and Action Buttons -->
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Selection Count -->
                    <TextBlock Grid.Column="0"
                               x:Name="txtSelectionCount"
                               Text="No documents selected"
                               FontSize="12" FontWeight="SemiBold"
                               VerticalAlignment="Center"
                               Foreground="#2C3E50"/>

                    <!-- Action Buttons -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button x:Name="btnOk" Content="OK"
                                Click="btnOk_Click"
                                Padding="20,8" Margin="0,0,10,0"
                                Background="#27AE60" Foreground="White"
                                FontWeight="SemiBold"
                                BorderThickness="0"
                                IsEnabled="False"/>
                        <Button x:Name="btnCancel" Content="Cancel"
                                Click="btnCancel_Click"
                                Padding="20,8"
                                Background="#95A5A6" Foreground="White"
                                FontWeight="SemiBold"
                                BorderThickness="0"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
