<Window x:Class="Alfresco.App.Windows.GroupDrilldownWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Pretraga unutar grupe"
        Height="520" Width="750"
        WindowStartupLocation="CenterOwner"
        Loaded="Window_Loaded">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2C3E50" Padding="12" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock x:Name="txtGroupTitle"
                           Foreground="White" FontSize="16" FontWeight="Bold"/>
                <TextBlock x:Name="txtGroupStats"
                           Foreground="#ECF0F1" FontSize="12" Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <!-- Opcija A: Uzmi sve -->
        <Border Grid.Row="1" Background="#F0EAF8" BorderBrush="#C39BD3" BorderThickness="1"
                CornerRadius="4" Padding="10" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Opcija A: Uzmi sve varijante" FontWeight="SemiBold" FontSize="13"/>
                    <TextBlock x:Name="txtAllVariantsDesc"
                               FontSize="11" Foreground="#666" Margin="0,3,0,0"/>
                </StackPanel>
                <Button Grid.Column="1" x:Name="btnAddAll"
                        Content="Dodaj sve varijante"
                        Background="#8E44AD" Foreground="White"
                        Padding="14,7" FontSize="12" BorderThickness="0"
                        Cursor="Hand" Click="btnAddAll_Click"/>
            </Grid>
        </Border>

        <!-- Opcija B: Filter po broju računa -->
        <Border Grid.Row="2" Background="#EAF4F8" BorderBrush="#85C1E9" BorderThickness="1"
                CornerRadius="4" Padding="10" Margin="0,0,0,8">
            <StackPanel>
                <TextBlock Text="Opcija B: Filter po broju računa" FontWeight="SemiBold" FontSize="13"
                           Margin="0,0,0,6"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="160"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Broj počinje sa:"
                               VerticalAlignment="Center" Margin="0,0,8,0" FontSize="12"/>
                    <TextBox Grid.Column="1" x:Name="txtFilter"
                             FontSize="12" Padding="5,4"
                             ToolTip="npr. '5' → matchuje sve varijante koje počinju sa 5"
                             KeyDown="txtFilter_KeyDown"/>
                    <Button Grid.Column="2" x:Name="btnSearch"
                            Content="Pretraži" Margin="8,0,0,0"
                            Padding="12,6" FontSize="12"
                            Click="btnSearch_Click"/>
                    <TextBlock Grid.Column="3" x:Name="txtFilterHint"
                               Text="" Foreground="#666" FontSize="11"
                               VerticalAlignment="Center" Margin="10,0,0,0"/>
                    <Button Grid.Column="4" x:Name="btnAddFilter"
                            Content="Dodaj filter"
                            Background="#2980B9" Foreground="White"
                            Padding="12,6" FontSize="12" BorderThickness="0"
                            Cursor="Hand" IsEnabled="False"
                            Click="btnAddFilter_Click"/>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Lista varijanti (rezultat pretrage) -->
        <Border Grid.Row="3" BorderBrush="#DDD" BorderThickness="1" CornerRadius="3">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Grid Header -->
                <Border Grid.Row="0" Background="#F5F5F5" BorderBrush="#DDD" BorderThickness="0,0,0,1">
                    <Grid Margin="8,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="100"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Naziv" FontWeight="SemiBold" FontSize="12"/>
                        <TextBlock Grid.Column="1" Text="Br. dokumenata" FontWeight="SemiBold"
                                   FontSize="12" HorizontalAlignment="Right"/>
                    </Grid>
                </Border>

                <ListView Grid.Row="1" x:Name="lstVariants"
                          BorderThickness="0"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="8,4"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="100"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding Naziv}" FontSize="12"/>
                                <TextBlock Grid.Column="1"
                                           Text="{Binding BrojDokumenata, StringFormat=N0}"
                                           FontSize="12" HorizontalAlignment="Right" Foreground="#555"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>

        <!-- Paginacija -->
        <Grid Grid.Row="4" Margin="0,6,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" x:Name="btnPrev" Content="◀ Prev"
                    Padding="10,5" IsEnabled="False" Click="btnPrev_Click"/>
            <TextBlock Grid.Column="1" x:Name="txtPageInfo"
                       VerticalAlignment="Center" Margin="10,0" FontSize="12"/>
            <TextBlock Grid.Column="2" x:Name="txtFoundCount"
                       VerticalAlignment="Center" FontSize="11" Foreground="#666"/>
            <Button Grid.Column="3" x:Name="btnNext" Content="Next ▶"
                    Padding="10,5" IsEnabled="False" Click="btnNext_Click"/>
        </Grid>

        <!-- Footer dugmad -->
        <Grid Grid.Row="5" Margin="0,10,0,0">
            <Button x:Name="btnClose" Content="Zatvori"
                    HorizontalAlignment="Right"
                    Padding="14,7" FontSize="12"
                    Click="btnClose_Click"/>
        </Grid>
    </Grid>
</Window>
